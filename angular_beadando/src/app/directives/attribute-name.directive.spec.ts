import { AttributeNameDirective } from './attribute-name.directive';

describe('AttributeNameDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeNameDirective();
    expect(directive).toBeTruthy();
  });
});
